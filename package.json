{
  "name": "ez-stablecoin",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "pnpm --filter \"./packages/*\" --parallel run start",
    "format-and-lint": "pnpm --filter \"./packages/*\" --parallel run format-and-lint"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@shadcn/ui": "^0.0.4"
  }
}
